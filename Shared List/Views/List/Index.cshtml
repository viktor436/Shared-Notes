@model List<Shared_List.Models.UserList>

<h2>Shared Lists</h2>

@if (Model.Count > 0)
{
    <table>
        <thead>
            <tr>
                <th>Title</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var userList in Model)
            {
                <tr>
                    <td>@userList.List.Title</td>
                    <td>
                        <a href="@Url.Action("Edit", "List", new { id = userList.ListId })">Edit</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No shared lists found.</p>
}
